package Models;

public class DemoBook extends Book{
    public DemoBook(String ISBN, String title, double price, int publishingYear) {
        super(ISBN, title, price, publishingYear);
    }
}
